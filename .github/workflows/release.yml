name: Automated Release System

on:
  pull_request:
    types:
      - closed
    branches:
      - main

jobs:
  detect-release-commit:
    if: github.event.pull_request.merged == true
    uses: ./.github/workflows/steps/detect-release-commit.yml

  extract-version-changelog:
    needs: detect-release-commit
    uses: ./.github/workflows/steps/extract-version-changelog.yml

  create-tag:
    needs: extract-version-changelog
    uses: ./.github/workflows/steps/create-tag.yml

  build-outputs:
    needs: create-tag
    uses: ./.github/workflows/steps/build-outputs.yml

  create-release:
    needs: build-outputs
    uses: ./.github/workflows/steps/create-release.yml

  notify:
    needs: create-release
    uses: ./.github/workflows/steps/notify.yml